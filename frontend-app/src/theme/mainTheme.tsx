import { createTheme } from "@mui/material/styles";

export let theme = createTheme({
  palette: {
    primary: {
      main: "#930A11",
    },
    secondary: {
      main: "#F7E9D2",
    },
  },
});
